
# This class takes a mutitple similar routes and figures out how to make a grid
# of all stops, even though the individual routes may have different stops.

class StopGridGenerator

  def initialize(array, other_array)
    @array, @other_array = array, other_array
  end

  # This assumes that the starting element is the same
  def superset
  end

end
